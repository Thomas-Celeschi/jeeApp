<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- pour utiliser les annotations -->
    <context:annotation-config />

    <!-- Un compteur -->
    <bean id="counter" class="myapp.xml.Counter" />

    <!-- Usine pour messages vides (par défaut) -->
    <bean id="factory" class="myapp.xml.MessageFactory"
          primary="true">
        <property name="counter" ref="counter" />
    </bean>

    <!-- Usine pour les messages "Hello " -->
    <bean id="helloFactory" class="myapp.xml.MessageFactory">
        <property name="messageBase">
            <value>Hello </value>
        </property>
        <property name="counter">
            <bean class="myapp.xml.Counter" />
        </property>
    </bean>

    <!-- Usine pour les messages "Bye " -->
    <bean class="myapp.xml.MessageFactory">
        <qualifier value="bye" />
        <property name="messageBase">
            <value>Bye </value>
        </property>
        <property name="counter" ref="counter" />
    </bean>

</beans>
